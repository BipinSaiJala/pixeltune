name: workflow 
on: 
  push:
    branches:   
    - master
    - feature
jobs:
  unittesting:
    runs-on: ubuntu-latest

    steps:
      - name: git checkout
        uses: actions/checkout@v4

      - name: unit testing
        run: npm install
