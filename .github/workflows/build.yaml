name: first workflow
on: 
  - push

jobs:
  practice:
    runs-on: ubuntu-latest
    steps:
      - name: list the files
        run: ls

      - name: create the files
        run: |
          touch deepu.txt
          echo "hello deepu" > deepu.txt

      - name: open files
        run: |
          grep -i deepu.txt
          cat deepu.txt



