apiVersion: v1
kind: Service
metadata:
  name: {{ include "pixeltune-chart.fullname" . }}
  labels:
    {{- include "pixeltune-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pixeltune-chart.selectorLabels" . | nindent 4 }}
