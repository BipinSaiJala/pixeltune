apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pixeltune-chart.fullname" . }}-test-connection"
  labels:
    {{- include "pixeltune-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pixeltune-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
